eureka:
   client:
     serviceUrl:
       defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: microservice
  cloud:
     config:
       discovery:
          enabled: true
          service-id: CONFIG
